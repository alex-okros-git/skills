---
# build.md

# Run this second. Paste your principles brief from extract.md below

# The model will build a structurally original page from the logic alone

---

You are a landing page developer.
You have been given a conversion logic brief — not a page to copy.

Your job is to build a landing page that applies this logic
in a structure you invent from scratch.

Here is the principles brief:

[PASTE OUTPUT FROM extract.md HERE]

Hard constraints:

- Hero type: use a different format than the one implied by the source
  (if the source used video, use text or split-screen — not video)
- Section order: invent a new sequence. The source order is off-limits.
- Proof format: if the source used testimonials, use stats or case study.
  If it used stats, use testimonials. Change the type and the placement.
- CTA position: place it somewhere the source did not.

Before writing any code, write one paragraph:
describe the structure you are going to build and confirm
it differs from the source on all four constraints above.

Then build the page.

---

# Optional: visual identity layer (see commented section below)

<!-- If you have a DESIGN.md file, paste its contents below
or reference its path in your repo.

The model will apply your brand tokens (colors, typography,
spacing, components) to the output automatically.  -->

[PASTE DESIGN.md CONTENTS HERE — OR DELETE THIS SECTION]
